title: ACI tenant
agents: special
catalog: hw
license: GPLv2
distribution: check_mk
description:
  ACI tenant health Check:
  {'/api/node/class/fvTenant.json?rsp-subtree-include=health'}

  There are configurable {{WARN}} and {{CRIT}} thresholds for the health value.

item:
  One item per tenant.

discovery:
  One service with the tenant name will be created.

